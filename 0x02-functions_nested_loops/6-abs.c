#include "main.h"

/**
 * _abs - calculate absolute value of an int
 *
 *@n: character to compare
 *
 * Return: always (0) success
 */

int _abs(int n)
{
	if (n < 0)
	{
		return (-n);
	}
	else
	{
		return (n);
	}
}

