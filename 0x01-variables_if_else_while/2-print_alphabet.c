#include <stdio.h>

/**
 * main - Start of program
 *
 * Return: Always (0) success
 *
 */


int main(void)
{
	char letter;

	for (letter = 'a'; letter <= 'z'; letter++)
	{
		printf("%c ", letter);
	}

	return (0);
}
